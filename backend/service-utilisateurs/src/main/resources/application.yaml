server:
  port: 8081

spring:
  application:
    name: service-utilisateurs
  datasource:
    url: jdbc:postgresql://localhost:5432/agriconnectdb
    username: agriconnect
    password: agriconnect237
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

jwt:
  secret: "agriconnect@2026#camer123456789"
  expiration: 86400000